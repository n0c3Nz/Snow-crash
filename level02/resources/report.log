#!/bin/bash
#
# Al introducirme en este usuario encuentro en su home
# un fichero .pcap que se que es una captura de red asi
# que utilizo wireshark para analizar el tráfico, tras
# revisar la data veo que lo que se acontece es un
# intento de login entre srv/client con el usuario:
# levelX
# contraseña:
# ft_wandr...NDREl.L0L
#
# Veo por la información en hexadecimal que los carácteres '.' son 7f -> 127 -> DEL
# Se representan como '.' al no poder ser interpretados por ascii, quitamos un carácter
# por punto y queda:
# ft_waNDRELOL
# Compruebo si es la correcta y
# efectivamente.
#
#
# (Para seguir la conversación en claro en WireShark usar: CTRL + ALT + SHIFT + T )
